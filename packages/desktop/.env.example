# === Supabase (REQUIRED for desktop auth) ===
# Same as app .env — these are read in the Electron main process during OAuth
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
